package com.jstarczewski.Game.GamesSettings;

public enum MoveOrder {

    /*
     * FIRST_SECOND => first player moves first, second player moves second
     * SCeOND_FIRST => second player moves first, first player moves second
     */

    FIRST_SECOND, SECOND_FIRST;
}
